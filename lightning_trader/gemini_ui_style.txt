請擔任 UI/UX 專家（參考 `ui-ux-pro-max-skill/SKILL.md`），幫我全面翻新 `frontend/src/` 的色系與樣式，以符合最新指示的「大戶投」APP 設計語彙。

核心風格要求如下：
1. **主背景色 (Background)**：改為非常有質感的「深邃藏青色 / 暗藍色」(例如 Tailwind 的 `slate-900` 或 `gray-900` 帶一點點藍，#1C2331 或類似色系)。請摒棄原本過於亮麗的賽博龐克藍色，改用沉穩、專業的證券科技風格。
2. **主視覺點綴色 (Primary Accent)**：改為「尊爵亮金 / 橘黃色」(例如 #D4AF37, #E5A344，近似大戶投的 LOGO 顏色)。這個顏色請用於重要按鈕、焦點 Tab 或是 Highlight 數字。
3. **強調 / 警告色 (Danger/Sell)**：保留或優化為大戶投常見的「深磚紅色」(台灣股市下跌為綠、上漲為紅，請符合台股習慣，上漲使用鮮明但不刺眼的紅色如 `#EF4444`，下跌使用清脆的綠色 `#10B981`)。
4. **介面質感 (Texture)**：保留毛玻璃 (Glassmorphism)，但要讓毛玻璃的底色也是深藍色基底 (`bg-slate-800/80` 等)，邊框用極細的淺色邊框 (`border-slate-700/50`) 勾勒。

任務：
- 全面修改 `frontend/src/index.css` 裡的 css variables (`--color-XXX`)，將色盤調整為上述要求。
- 檢查 `frontend/src/components/LoginPanel.tsx` 與 `DOMPanel.tsx`，把裡面寫死的 `bg-gradient-to-r from-blue-600 to-cyan-600` 等賽博龐克色，改成符合大戶投的品牌金色 (`from-yellow-600 to-orange-500` 等) 與按鈕樣式。
- 讓字體、排版更貼近專業理財 APP，而不是電競軟體。
- 不用詢問我，直接修改原始碼。
